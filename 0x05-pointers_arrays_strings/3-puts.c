#include "main.h"
#include <stdio.h>

/**
 * _puts - Prints a string to stdout followed by a newline
 * @str: the string to be printed
 * Return: void
 */

void _puts(char *str)
{
	printf("%s\n", str);
}
