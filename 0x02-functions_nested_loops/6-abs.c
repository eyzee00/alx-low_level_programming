#include "main.h"

/**
 * _abs - function
 * @x: integer
 * Description: returns the absolute value of x
 * Return: absolute value of x
 */

int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-x);
}
